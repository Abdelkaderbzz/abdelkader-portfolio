[build]
  command = "bun run build"
  # Next.js Runtime expects `.next` (never Vite's `dist`).
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
